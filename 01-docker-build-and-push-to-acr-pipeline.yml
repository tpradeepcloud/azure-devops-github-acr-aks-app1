# Docker
- task: QualysCSConnector@1
  inputs:
    QualysApiServer: 'https://qualysguard.qg1.apps.qualys.in'
    QualysApiUsername: 'gftve8pt'
    QualysApiPassword: 'iloveINDIA@2717'
    imageID: 'app1nginx/31'
    dockerUrl: 'unix:///var/run/docker.sock'
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- main

resources:
- repo: self

variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: 'b63ded32-d943-4a3e-be2d-85187351451a'
  imageRepository: 'app1nginx'
  containerRegistry: 'aksdevopsacrdev.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  tag: '$(Build.BuildId)'
  
  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:  
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
